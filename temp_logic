??  **************************************used this for early testing ensure that the words.txt file would work for pulling words for array:


					 
					 	//echo 'Count: ' . count($words) . '<br />';
					 	//echo $words[40000];
						//two lines above used to testing, and proved that the words.txt file and the logic to pull a word do work.








//    ******************* Creates the array that contains the word list for the password



if ($words = file('words.txt')) {
	//file() creates an array from the .txt file, turning every line into an element in the array

	$selected_words = [];  
	//create an array, then create a count, count based on the number input on the form, and add words to the array

		for ($i=0; $i < $count ; $i++) { 
		 	# generate randome number form 0 to dictionary size

		 	$max = rand(0, count($selected_words)-1);  //determine the size of the array
		 	$rand = rand(0, $max);  //get a random number between zero and the size of the array

		 	//add item to array:
		 	$word = $words [$rand];

		 	echo $word . '<br />';

		 	//array_push($selected_words, $word);

		 } 


	
	if ($uppercase) {
		# code...
	}

	if ($symbol) {
		# code...
	}

	if ($number) {
		# code...
	}

	

	
}



